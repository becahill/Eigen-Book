# Source Directory

The matching core is currently header-only under `include/` so templates and hot-path code remain easy to audit. Add non-template implementation files here when a future component benefits from separate compilation.
